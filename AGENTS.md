# AGENTS.md

- 常に日本語でやりとりする。
- UserScript メタデータの `@version` は、ユーザーから明示的に依頼されない限り変更しない。
- `@version` の変更が必要そうに見える場合でも、先にユーザーへ確認する。

## Git commit

- ユーザーから git commit を任された場合は、変更内容を確認してからコミットする。
- コミットメッセージは英語で書く。
- コミットメッセージは、実装差分の細部ではなく、機能追加・バグ修正・ユーザーに見える挙動の粒度で要約する。
- タイトル行は 80 文字以内にする。
- タイトル行は変更したファイル名を角括弧で始める。例: `[samplescript.user.js] Fix sample behavior.`
- タイトル行では `Add`、`Fix`、`Update`、`Remove`、`Refactor` などの簡潔な命令形の動詞を使う。
- タイトルだけで明確に説明できる変更は、タイトル行のみを書く。
- 詳細が必要な場合は、1 行目をタイトル、2 行目を空行、3 行目以降を詳細にする。
- 詳細セクションは、タイトルに収まらない重要な背景・挙動変更・理由の説明にだけ使う。コードレベルの編集一覧は書かない。
- 複数の独立したユーザー向け変更や重要な理由がない限り、1 行の簡潔なタイトルを優先する。
