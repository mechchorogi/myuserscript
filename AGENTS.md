# AGENTS.md

- 常に日本語でやりとりする。
- UserScript メタデータの `@version` は、ユーザーから明示的に依頼されない限り変更しない。
- `@version` の変更が必要そうに見える場合でも、先にユーザーへ確認する。
- 実装意図や将来の保守で重要な前提を英語のコードコメントとして残す。

## Git commit

- タイトル行は変更したファイル名を角括弧で始める。例: `[samplescript.user.js] Fix sample behavior.`

## バージョニング

- `@version` はユーザーから明示的に依頼されない限り変更しない。
- ユーザーが具体的なバージョン番号を指定せず「バージョンを上げて」とだけ依頼した場合は、前回バージョンからの累積差分を確認したうえで、以下の基準で分類して提案する。ユーザーの確認を得てから変更する。
  - 新機能追加を含む → **MINOR**（このプロジェクトの過去のbumpは全てMINOR）
  - バグ修正・整形など機能追加を伴わない変更のみ → **PATCH**
  - `@match`・保存キー形式・既存キーボードショートカットなど非互換な変更を含む → **MAJOR**（個別に要確認）
- 提案時は「現在のバージョン」「提案するバージョン」「一行の理由」を示す。
